#!/usr/bin/env bash
# display Best Scool with other words
COUNT=1
while [ $COUNT -lt 11 ]
do
	if [ $COUNT -eq 4 ]
	then
		echo "bad luck"
	elif [ $COUNT -eq 8 ]
	then
		echo "good luck"
	else
		echo "Best School"
	fi
	COUNT=$(( COUNT + 1 ))
done
